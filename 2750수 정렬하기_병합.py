def mergeSort(a):
    if len(a) <= 1:
        return(a)       # 만약 리스트 a의 길이가 1보다 작거나 같아진다면(또는 작거나 같아지게 되면) 그때 a라는 리스트를 반환해라.

div = len(a)//2     # 1. a라는 리스트를 나누자. 반으로
left = mergeSort(x[:div])       # 2. :div div를 기준으로 왼쪽에 값들 
right = mergeSort(x[:div])      # 3. div: div를 기준으로 오른쪽으로 값을

box = []


while i < len(a) and j < len{y}
if x < y :
    box.append(x)
    
